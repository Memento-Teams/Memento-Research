# Experiment Report Standard

The final report must state status, stop reason, duration, command, working
directory, monitor backend, target metric, best/latest metric values, system
snapshot, and stdout/stderr tails when available.
